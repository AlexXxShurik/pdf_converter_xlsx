# AGENTS.md — извлечение данных из PDF-каталога AccKee в XLSX

## Исходные данные

| Файл                | Описание |
|---------------------|----------|
| `data/AccKee2025.pdf` | Входной каталог, 84 стр., альбомная (~1207×825 pt). **Есть текстовый слой** — все значения таблиц точные (через `page.get_text('words')`). Стр. 2, 57, 74 (0-based) без текстового слоя → для них OCR-fallback. |
| `result.xlsx`       | Выходной файл (перезаписывается). |

Ключевое открытие: **в PDF есть векторные линии сетки** (`page.get_drawings()` → ~1580 объектов).
Это точный источник границ таблиц, колонок и объединённых ячеек. Использовать их вместо
«угадывания» структуры по OCR.

## Структура страницы (проверено на стр. 4, 0-based=3)

На странице 4 сверлильные таблицы + 1 таблица Spare Parts внизу справа:

| Таблица | x-диапазон | y-диапазон | Модели |
|---------|-----------|-----------|--------|
| T1 | 55–306 | 191–773 | 2D10-22SP04 … 2D33.5-71SP09 |
| T2 | 306–559 | 191–715 | 2D34-72SP09/11 … 2D60-128SP09 |
| T3 | 640–888 | 191–773 | 3D10-32SP04 … 3D33.5-105SP09 |
| T4 | 895–1140 | 191–715 | 3D34-106SP09/11 … 3D60-188SP90 |
| SP  | 895–1140 | 726–773 | Spare Parts |

Колонки сверлильной таблицы (T1):
`Dc | Ds | Specification/Item | Store | L | l1 | l2 | l3 | Applicable Insert | kg`

x-границы колонок T1: `55.4, 85.1, 101.1, 160.2, 175.7, 193.4, 208.5, 223.7, 236.9, 290.6, 306.4`.
Вертикальные длинные линии (`y1-y0 > 450`) дают границы колонок всех таблиц.

## Объединённые ячейки (критично)

- Объединены колонки **Ds, l3, Applicable Insert**. Значение объединённой ячейки
  **дублируется на все строки диапазона**.
- Границы объединений берутся из **горизонтальных линий сетки, покрывающих x-центр колонки**.
  У разных колонок границы разные (т.е. диапазоны объединений могут не совпадать по колонкам).
- Пример T1: Ds = `20` (6 строк 2D10..2D12.5), `20/25` (9 строк 2D13..2D17.5),
  `25` (16 строк 2D18..2D25.5), `32` (17 строк 2D26..2D33.5); в T2 Ds=32 ещё 12 строк (итого 29).
- Значение `/` — это диапазон, вставлять как есть: `20/25`, `44/56`.
- Если в одной объединённой ячейке несколько токенов — склеивать **через пробел** и
  дублировать на все строки. Пример AI: `SP..07T308 SP..090408` (в одной ячейке на 2 строки,
  обе пластины подходят). Это НЕ ошибка таблицы.

Горизонтальные линии объединений T1:
- Ds/l3: `221.8, 290.7, 394.1, 577.9, 773.1`
- AI: `221.8, 290.7, 359.6, 497.5, 635.3, 658.3, 773.1`

## Заголовки таблиц

- Заголовки в текстовом слое отсутствуют (только в растре) → **OCR узкой полосы шапки
  по каждой колонке** (кроп y≈196–214, zoom 4). Работает надёжно:
  `Dc`, `Ds`, `Specification/Item`, `Store`, `L`, `l1`, `l2`, `l3`, `Applicable Insert`, `kg`.
- Слабые места OCR: `L`, `l1`, `l2`, `l3` могут читаться как `-`, `Q2`, `Q3` и т.п.
  Такие «плохие» метки при сравнении наборов заголовков **игнорировать** (правило от пользователя).
- **Если `L` не распознался (OCR дал `-`/пусто) — он считается ненадёжным** и при
  сравнении наборов заголовков не сверяется (то же правило, что для l1/l2/l3).
- Spare Parts шапка: `type | diameter | Blade model | Screw | Wrench` (низ страницы, y≈735).
  Заголовки читаются из **текстового слоя** (y≈742–744), не OCR.
  **Screw — объединение 3 физических колонок**: винт `M1.8/M2`, знак `×` («на», OCR
  даёт `X`/`x`), число `3.6/4`. Цифры разделены намеренно (чтобы использовать в Excel).
  В XLSX — **заголовок Screw на 3 колонки** (merge в шапке), данные раздельно.
  Колонка `type` (`SP`) объединяет все 3 строки таблицы — значение дублируется.
  Рамки SP: y=747.6/773.1, x=900–1141; колонки по линиям x=934, 979, 1033, 1070, 1082, 1107.

## Решения пользователя (зафиксированы)

1. **Строка XLSX = модель**, колонки как в каталоге: `Dc | Ds | Spec | Store | L | l1 | l2 | l3 | AI | kg`.
   Store `●/○` сохраняется как есть.
2. **Объединённые значения повторяются** на каждой строке диапазона (не merge_cells).
3. **Spare Parts → отдельный лист** `Spare Parts`.
4. **Группировка листов по заголовкам**: все таблицы с одинаковым набором заголовков → один лист
   (2D- и 3D-серии в один лист, т.к. заголовки совпадают). Сравнивать `L` и надёжные метки,
   не надёжные (l1/l2/l3, а также L при плохом OCR) не сверять.
   **Правило (уточнено пользователем):** две таблицы считаются одинаковыми, если
   совпало **общее количество колонок** И по значению и порядку совпало **≥ половины
   позиций** заголовков.
5. Числовые значения — числа в XLSX (float), не строки. Значения с `/` и `SP..` — строки.

## Pipeline (новый гибридный подход)

1. **Данные** — текстовый слой `page.get_text('words')` (точные токены и координаты).
2. **Сетка** — `page.get_drawings()`:
   - вертикальные линии → границы колонок/таблиц;
   - горизонтальные линии, покрывающие x-центр колонки → границы объединённых ячеек.
3. **Строки** — y-позиции токенов модели (Spec-колонка, паттерн `2D10-22SP04`).
4. **Объединения** — интервал между горизонтальными линиями колонки: все токены интервала
   склеиваются через пробел (или как есть с `/`), значение дублируется на строки интервала.
5. **Обычные колонки** (Dc, Spec, Store, L, l1, l2, kg) — значение = токен в той же строке.
6. **Заголовки** — OCR полосы шапки по колонкам (см. выше).
7. **Группировка листов** — по набору заголовков (без «плохих» меток).
8. **XLSX** — мультилистовой файл, числа как числа.
9. **OCR-fallback** — для стр. 2/57/74 (нет текстового слоя) OCR полных таблиц.

## Текущее состояние

Готово (исследование):
- Структура стр. 4 полностью подтверждена: 4 сверлильные + SP, границы колонок и объединений,
  x-центры, паттерны моделей, OCR шапки по колонкам.
- Правила объединений и формат вывода согласованы с пользователем.

Готово (реализация):
- `converter/acc_parser.py` — гибридный парсер: `parse_page()` возвращает таблицы страницы.
  `_find_tables()`: таблицы по кластерам горизонтальных линий-«рамок» (группировка по x0,
  top=min y, bottom=первая рамка после последней модели в панели). Порог span рамки = 100
  (раньше 150 — не ловил 3-колоночные KSD/C-таблицы стр. 8, span≈109). Верхняя рамка может быть
  y≈221.8 (обычные стр.) или y≈187.8 (WC-страницы стр. 9-11). Колонки = вертикальные линии
  в диапазоне (в т.ч. короткие — KOD/WC таблицы с нижней рамкой y≈325). SP-зона (y 730-780)
  исключается ТОЛЬКО по совпадению полного (x0,x1,y) — иначе теряются нижние рамки обычных
  таблиц с той же y-координатой (баг на стр. 4: T1/T3 bottom обрезался на 577.9). Модели:
  `MODEL_RE` (2D/3D/4D/5D … SP/SOMT, `2D10.5-23SP04`, `3D36-112SOMT11`),
  `KOD_MODEL_RE` (`KOD130-3D-S20/25-SO05`), `KSD_MODEL_RE` (`KSD085-2D-S20-SP03`),
  `C_MODEL_RE` (`C20-2D13-29SP05`, `C32-3D38.7-121SP11` — 3-колоночные таблицы стр. 8).
  Объединения (Ds/l3/AI) — по горизонтальным линиям колонки, значение дублируется.
  **Объединённой колонка считается только если ≥2 ячеек** (есть промежуточные hlines);
  одна ячейка на всю таблицу (напр. kg) — НЕ объединяется (иначе kg склеивается в строку).
  `parse_page()` сохраняет `top`/`bottom` в результат — без этого `ocr_headers()` использует
  дефолт 221.8 и для KSD/C-таблиц (top=406.3) OCR даёт мусор ('H13/SKD6').
  SP: `_find_sp_tables()` — рамки y 730-780, `type` дублируется;
  строки кластеризуются по близости y (≤4) — на стр. 5 токены строки на y=752.1/752.5.
- `converter/acc_headers.py` — `ocr_headers()` (OCR полосы (top-45, top-5) — широкая, чтобы
  поймать и буквы L/l1/l2/l3 и субиндексы 1/2/3) + `sp_headers()` (заголовки SP из текстового
  слоя, полоса (y0-25, y0) над верхней рамкой — на стр. 4 y≈747, на стр. 5 y≈738).
- `converter/xlsx_writer.py` — мультилистовой, группировка по заголовкам: `_compatible()`
  сверяет ТОЛЬКО канонические метки (Dc, Ds, Spec, Store, L, AI, kg); l1/l2/l3 и мусорные
  OCR-метки ('Fig.', '8', 'Fi') не учитываются; совпало ≥ половины сверяемых позиций → один
  лист. Числа как float, Screw — merge в шапке на 3 колонки. `_sheet_name()`: 3 колонки с Dc →
  лист `C-KSD Series` (иначе 'Drilling1' от openpyxl, `/` в имени недопустим).
- `converter/pipeline.py`, `main.py` — `--pages "1-12, 15-32"`, список диапазонов.
- Прогон стр. 4-15 (1-based) → `results/result_4_15.xlsx`: листы `Drilling` (1291 строка,
  серии 2D/3D/4D/5D/KOD/KSD/WC/SOMT, все 1412 моделей текстового слоя распознаны),
  `Spare Parts` (6 строк) и `C-KSD Series` (121 строка, 3 колонки Dc/Ds/Spec, стр. 8).
  Проверено: 0 склеенных значений, 0 пустых Ds/AI/Spec. Дубли SOMT 3D (стр. 7 и 15,
  9 одинаковых строк) — реальное повторение в каталоге, оставлено как есть (решение пользователя).
  kg у больших диаметров (напр. 2D61-2D80 WC06, 4D74+ WC06) в каталоге отсутствует — пустое.

Не начато:
- Стр. 1-3, 16-32 каталога (часть — без текстового слоя).
- OCR-fallback для стр. 2/57/74 (0-based, нет текстового слоя).

## Структура кода

```
pdf_converter_xlsx/
├── main.py                 # CLI: PDF → result.xlsx, --pages "1-12, 15-32"
├── converter/
│   ├── pdf_to_images.py    # open_pdf/render_page
│   ├── ocr_engine.py       # PaddleOCR, ocr_blocks/ocr_rotated_cw
│   ├── preprocess.py       # OpenCV (detect_dots и пр.)
│   ├── table_geometry.py   # старая DeskAr-геометрия (OCR-подход)
│   ├── parser.py           # старый парсер DeskAr
│   ├── validator.py        # старый dedup
│   ├── xlsx_writer.py      # старый writer (однолистовой)
│   └── pipeline.py         # старая обвязка
└── probes/                 # скрипты разведки стр. 4
```

Старые модули заточены под DeskAr (OCR-подход, один лист) — для AccKee их переработать
под гибридный подход (текст + векторные линии) и мультилистовой вывод.

## Порядок работы дальше

1. Написать новый парсер страницы: таблицы по вертикальным линиям, строки по моделям,
   значения по токенам + объединения по горизонтальным линиям, заголовки по OCR колонок.
2. Группировка таблиц в листы по заголовкам; writer с несколькими листами.
3. Обновить main.py и pipeline.py (диапазоны страниц).
4. Прогнать стр. 4 (0-based 3) → `result.xlsx`, остановиться и показать пользователю.
5. Полный прогон каталога, сверить с ожиданиями пользователя, OCR-fallback для стр. 2/57/74.